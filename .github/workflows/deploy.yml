name: Deploy

# Wire to your host (Vercel, Netlify, container registry, etc.)
on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Configure deploy steps for apps/dashboard build output (dist)."
