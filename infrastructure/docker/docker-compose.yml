# Local stack (stubs) — extend with real images as services land.
version: '3.9'
services:
  api-gateway:
    build:
      context: ../..
      dockerfile: infrastructure/docker/Dockerfile.gateway
    ports:
      - '8787:8787'
    environment:
      - PORT=8787
