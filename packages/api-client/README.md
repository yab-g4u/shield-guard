# @shieldguard/api-client

Shared HTTP primitives for calling ShieldGuard REST endpoints from the dashboard, workers, or internal tools.

The public **npm SDK** (`@shieldguard/sdk`) remains the supported surface for external developers; this package is for first-party code reuse.
