/**
 * Evaluation types and contracts (import `ShieldGuard` from the package root).
 */
export type {
  EvaluateTransactionInput,
  EvaluateTransactionResponse,
  ShieldDecision,
  ShieldPolicy,
  ShieldConfig,
} from './types';
