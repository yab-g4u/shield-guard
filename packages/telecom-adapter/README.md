# @shieldguard/telecom-adapter

Maps operator-specific APIs (CAMARA, GSMA Open Gateway, private MNO integrations) into ShieldGuard’s internal signal model.

Implementations will live here so `fraud-engine-service` and `packages/sdk` stay transport-agnostic.
