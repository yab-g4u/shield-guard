# api-gateway

Public REST/GraphQL edge (future). Routes requests to `fraud-engine-service`, `analytics-service`, and `auth-service`.

Current tree is a **health-check stub** so the monorepo layout matches production architecture.
